export default function Loading(){
    return <p>
        Loading Data .....
    </p>
}

//Behind the scene this loading.js file activate streaming, so basically this data will be streamed from the server to the client automatically.